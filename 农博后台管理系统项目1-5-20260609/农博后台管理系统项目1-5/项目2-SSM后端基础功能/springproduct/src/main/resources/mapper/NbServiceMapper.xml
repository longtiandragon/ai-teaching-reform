<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.nong.mapper.NbServiceMapper">

    <resultMap id="BaseResultMap" type="org.nong.entity.NbService">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="image" column="image"/>
        <result property="resume" column="resume"/>
        <result property="content" column="content"/>
        <result property="category" column="category"/>
        <result property="provider" column="provider"/>
        <result property="phone" column="phone"/>
        <result property="price" column="price"/>
        <result property="status" column="status"/>
        <result property="address" column="address"/>
        <result property="orderCount" column="order_count"/>
        <result property="rating" column="rating"/>
    </resultMap>

    <sql id="selectColumns">
        id, title, image, resume, content, category, provider, phone,
        price, status, address, order_count, rating
    </sql>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="selectColumns"/>
        FROM nb_service
        <where>
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
            <if test="provider != null and provider != ''">
                AND provider LIKE CONCAT('%', #{provider}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY order_count DESC, id DESC
        <if test="offset != null and pageSize != null">
            LIMIT #{offset}, #{pageSize}
        </if>
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*) FROM nb_service
        <where>
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
            <if test="provider != null and provider != ''">
                AND provider LIKE CONCAT('%', #{provider}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="selectColumns"/>
        FROM nb_service WHERE id = #{id}
    </select>

    <insert id="insert">
        INSERT INTO nb_service (
            id, title, image, resume, content, category, provider, phone,
            price, status, address, order_count, rating
        ) VALUES (
            #{id}, #{title}, #{image}, #{resume}, #{content}, #{category}, #{provider}, #{phone},
            #{price}, #{status}, #{address}, #{orderCount}, #{rating}
        )
    </insert>

    <update id="update">
        UPDATE nb_service
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="image != null">image = #{image},</if>
            <if test="resume != null">resume = #{resume},</if>
            <if test="content != null">content = #{content},</if>
            <if test="category != null">category = #{category},</if>
            <if test="provider != null">provider = #{provider},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="price != null">price = #{price},</if>
            <if test="status != null">status = #{status},</if>
            <if test="address != null">address = #{address},</if>
            <if test="orderCount != null">order_count = #{orderCount},</if>
            <if test="rating != null">rating = #{rating},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM nb_service WHERE id = #{id}
    </delete>

    <delete id="deleteBatch">
        DELETE FROM nb_service WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>

