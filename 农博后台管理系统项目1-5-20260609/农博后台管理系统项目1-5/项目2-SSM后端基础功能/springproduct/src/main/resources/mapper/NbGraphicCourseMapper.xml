<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.nong.mapper.NbGraphicCourseMapper">

    <resultMap id="BaseResultMap" type="org.nong.entity.NbGraphicCourse">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="teacher" column="teacher"/>
        <result property="resume" column="resume"/>
        <result property="content" column="content"/>
        <result property="image" column="image"/>
        <result property="category" column="category"/>
        <result property="browseNum" column="browse_num"/>
        <result property="publishStatus" column="publish_status"/>
        <result property="publishTime" column="publish_time"/>
        <result property="recommend" column="recommend"/>
        <result property="createdTime" column="created_time"/>
        <result property="updatedTime" column="updated_time"/>
    </resultMap>

    <sql id="selectColumns">
        id, title, teacher, resume, content, image, category, browse_num,
        publish_status, publish_time, recommend, created_time, updated_time
    </sql>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="selectColumns"/>
        FROM nb_graphic_course
        <where>
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="teacher != null and teacher != ''">
                AND teacher LIKE CONCAT('%', #{teacher}, '%')
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
            <if test="publishStatus != null">
                AND publish_status = #{publishStatus}
            </if>
            <if test="recommend != null">
                AND recommend = #{recommend}
            </if>
        </where>
        ORDER BY created_time DESC
        <if test="offset != null and pageSize != null">
            LIMIT #{offset}, #{pageSize}
        </if>
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*) FROM nb_graphic_course
        <where>
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="teacher != null and teacher != ''">
                AND teacher LIKE CONCAT('%', #{teacher}, '%')
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
            <if test="publishStatus != null">
                AND publish_status = #{publishStatus}
            </if>
            <if test="recommend != null">
                AND recommend = #{recommend}
            </if>
        </where>
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="selectColumns"/>
        FROM nb_graphic_course WHERE id = #{id}
    </select>

    <insert id="insert">
        INSERT INTO nb_graphic_course (
            id, title, teacher, resume, content, image, category, browse_num,
            publish_status, publish_time, recommend, created_time, updated_time
        ) VALUES (
            #{id}, #{title}, #{teacher}, #{resume}, #{content}, #{image}, #{category}, #{browseNum},
            #{publishStatus}, #{publishTime}, #{recommend}, #{createdTime}, #{updatedTime}
        )
    </insert>

    <update id="update">
        UPDATE nb_graphic_course
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="teacher != null">teacher = #{teacher},</if>
            <if test="resume != null">resume = #{resume},</if>
            <if test="content != null">content = #{content},</if>
            <if test="image != null">image = #{image},</if>
            <if test="category != null">category = #{category},</if>
            <if test="browseNum != null">browse_num = #{browseNum},</if>
            <if test="publishStatus != null">publish_status = #{publishStatus},</if>
            <if test="publishTime != null">publish_time = #{publishTime},</if>
            <if test="recommend != null">recommend = #{recommend},</if>
            <if test="updatedTime != null">updated_time = #{updatedTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM nb_graphic_course WHERE id = #{id}
    </delete>

    <delete id="deleteBatch">
        DELETE FROM nb_graphic_course WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>

